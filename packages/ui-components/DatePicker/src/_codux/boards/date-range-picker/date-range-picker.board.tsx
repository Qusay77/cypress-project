import { createBoard } from "@wixc3/react-board";
import DateRangePicker from "../../../../components/DateRangePicker";

export default createBoard({
	name: "DateRangePicker",
	Board: () => <DateRangePicker />
});

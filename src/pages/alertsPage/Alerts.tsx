import AppLayout from "src/layout/AppLayout";
import { AlertsBody } from "./Theme";

const Alerts = () => {
	return (
		<AppLayout>
			<AlertsBody>Alerts</AlertsBody>;
		</AppLayout>
	);
};

export default Alerts;

import AppLayout from "src/layout/AppLayout";
import { DashboardBody } from "./Theme";

const Dashboard = () => {
	return (
		<AppLayout>
			<DashboardBody>Dashboard</DashboardBody>;
		</AppLayout>
	);
};

export default Dashboard;
